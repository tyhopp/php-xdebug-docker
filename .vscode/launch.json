{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Listen for Xdebug",
      "type": "php",
      "request": "launch",
      // "hostname": "0.0.0.0", // Required for Docker/WSL2
      "port": 9003,
      "pathMappings": {
			  "/tmp": "${workspaceFolder}"
			},
      "log": true
    }
  ]
}